# Copy this file to .env and fill in real values. The .env file is gitignored.
# spring-dotenv loads these into Spring's property resolution at startup.
DB_URL=jdbc:mysql://localhost:3306/tripmate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=root
DB_PASS=change_me
JWT_SECRET=base64-encoded-32-byte-secret
FRONTEND_URL=http://localhost:5173
KAFKA_BOOTSTRAP=localhost:9092
